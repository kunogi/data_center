import os

# ==========================================
# ⚙️ 配置中心 (改为绝对路径)
# ==========================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DB_PATH = os.path.join(BASE_DIR, 'quant_data.db')
BLACKLIST_FILE = os.path.join(BASE_DIR, 'blacklist.txt')
COMPLETED_FILE = os.path.join(BASE_DIR, 'completed_financial_codes.txt')

# 💥 【全新架构参数】连续获取过去几个季度的财报 
# 12个季度 = 3年，用于后续计算“连续3年ROE”、“连续3年盈利质量”等护城河指标
FINANCIAL_QUARTERS = 12
EXPIRE_DAYS = 1  # 💥 财报数据过期天数 (超过此时长，系统将重新拉取该股票的最新财报)

# 写死我们永远需要跟踪的核心指数 (保底名单)
CORE_INDICES = ['sh.000001', 'sz.399001', 'sz.399107', 'sh.000300', 'sz.399006', 'sh.000905', 'sh.000852', 'bj.899050']

# K线数据获取天数
DAILY_K_DAYS = 5000  # 💥 物理封印：永远保持足够大，覆盖数据库起点的全历史跨度！切勿改小！
# 如果你把它改成 365 会发生什么？
# 假设现在库里有贵州茅台 2012 年至今的 14 年完整数据。
# 明天茅台分红了，收盘价发生跳空。代码在比对重叠日时发现了价格偏移，触发了 need_full_reload = True 的自愈机制：
# 程序执行了 DELETE FROM daily_k_data WHERE code = '贵州茅台'。（茅台 14 年历史被清空了）
# 接着，程序按 DAILY_K_DAYS 重新拉取。如果你设的是 365，程序只会向 Baostock 请求茅台最近 1 年的最新前复权数据。
# 结果就是：这只股票在你的库里，前 13 年的 K 线永久性地灰飞烟灭了。 当你下次跑 _sim.py 回测 2016 年到 2020 年的行情时，系统会发现茅台“不存在”，直接导致回测失真。
# 💡 最终建议
# 既然已经构建了 10 年以上的完整历史底座，请永久保持 DAILY_K_DAYS = 5000（或者 4000 等足以覆盖你起点的天数），不要改小！
# 日常开销极小：每天全市场发生除权分红的股票只有寥寥几只（分红季可能多一点，也就几十只）。这几十只股票触发全量 5000 天重拉，对网络请求来说也就多花几十秒钟，完全不会拖垮你整体 10 分钟左右的日常增量同步进度。
# 收益极大：它能保证你的底层数据库像一个活体细胞一样，无论是新增还是自愈新陈代谢，永远都维持着 10 年以上的全生命周期完整性，时刻为实盘和回测提供最长跨度的火力支援。